<!DOCTYPE html>
<html lang="es" class="h-full">
<head>
    <meta charset="utf-8"/>
    <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
    <title>{{ title }} - Momomy Bakery</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#f0427c',
                        secondary: '#ff7eb3',
                    }
                }
            }
        }
    </script>
    <style>
        body { font-family: "Plus Jakarta Sans", sans-serif; }
        .glass { background: rgba(255, 255, 255, 0.8); backdrop-filter: blur(15px); }
    </style>
</head>
<body class="h-full bg-[#fdf2f5] flex items-center justify-center p-4">
    <div class="max-w-md w-full glass rounded-[3rem] border border-white/40 shadow-2xl p-8 space-y-8">
        <div class="text-center">
            <h1 class="text-2xl font-black text-slate-900">Únete a Momomy</h1>
            <p class="text-slate-500 font-medium text-sm">Crea tu cuenta para gestionar tus pedidos y cotizaciones</p>
        </div>

        {% if order %}
        <div class="bg-primary/5 p-4 rounded-2xl border border-primary/10 flex items-center gap-4">
            <div class="size-10 rounded-full bg-primary text-white flex items-center justify-center">
                <span class="material-symbols-outlined">shopping_bag</span>
            </div>
            <div>
                <p class="text-xs font-bold text-primary">Vincular Pedido #{{ "%06d"|format(order.id) }}</p>
                <p class="text-[10px] text-slate-500 uppercase font-black">Tu cuenta quedará ligada a este pedido</p>
            </div>
        </div>
        {% endif %}

        <form action="{{ base_url }}connect/register" method="POST" class="space-y-4">
            {% if order %}
                <input type="hidden" name="claim_token" value="{{ order.tracking_token }}">
            {% endif %}

            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="block text-xs font-bold text-slate-500 mb-1 ml-1">Nombre</label>
                    <input type="text" name="first_name" value="{{ order.customer_first_name }}" required
                           class="w-full px-4 py-3 rounded-2xl border border-slate-200 focus:ring-2 focus:ring-primary outline-none text-sm transition-all">
                </div>
                <div>
                    <label class="block text-xs font-bold text-slate-500 mb-1 ml-1">Apellido</label>
                    <input type="text" name="last_name" value="{{ order.customer_last_name }}" required
                           class="w-full px-4 py-3 rounded-2xl border border-slate-200 focus:ring-2 focus:ring-primary outline-none text-sm transition-all">
                </div>
            </div>

            <div>
                <label class="block text-xs font-bold text-slate-500 mb-1 ml-1">Email</label>
                <input type="email" name="email" value="{{ order.customer_email }}" required
                       class="w-full px-4 py-3 rounded-2xl border border-slate-200 focus:ring-2 focus:ring-primary outline-none text-sm transition-all">
            </div>

            <div>
                <label class="block text-xs font-bold text-slate-500 mb-1 ml-1">Teléfono</label>
                <input type="text" name="phone" value="{{ order.customer_phone }}" required
                       class="w-full px-4 py-3 rounded-2xl border border-slate-200 focus:ring-2 focus:ring-primary outline-none text-sm transition-all">
            </div>

            <div>
                <label class="block text-xs font-bold text-slate-500 mb-1 ml-1">Contraseña</label>
                <input type="password" name="password" required
                       class="w-full px-4 py-3 rounded-2xl border border-slate-200 focus:ring-2 focus:ring-primary outline-none text-sm transition-all shadow-inner">
            </div>

            <button type="submit" class="w-full py-4 bg-primary text-white rounded-2xl font-black text-sm shadow-lg shadow-primary/25 hover:scale-[1.02] active:scale-95 transition-all mt-4">
                CREAR MI CUENTA
            </button>
        </form>

        <p class="text-center text-xs text-slate-500 font-medium">
            ¿Ya tienes cuenta? <a href="{{ base_url }}connect/login" class="text-primary font-bold hover:underline">Inicia sesión</a>
        </p>
    </div>
</body>
</html>
