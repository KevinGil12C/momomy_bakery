<!DOCTYPE html>
<html lang="es" class="h-full">
<head>
    <meta charset="utf-8"/>
    <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
    <title>Mi Portal Momomy</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#f0427c',
                    }
                }
            }
        }
    </script>
    <style>
        body { font-family: "Plus Jakarta Sans", sans-serif; }
    </style>
</head>
<body class="bg-[#fdf2f5] min-h-full p-4 lg:p-12">
    <div class="max-w-5xl mx-auto space-y-8">
        <!-- Profile Header -->
        <div class="flex flex-col md:flex-row md:items-center justify-between gap-6">
            <div class="flex items-center gap-6">
                <div class="size-20 bg-primary text-white rounded-[2rem] flex items-center justify-center text-3xl font-black shadow-2xl shadow-primary/30">
                    {{ session.customer_name|slice(0, 1)|upper }}
                </div>
                <div>
                    <h1 class="text-3xl font-black text-slate-900">¡Hola, {{ session.customer_name }}!</h1>
                    <p class="text-slate-500 font-medium tracking-tight">Bienvenido a tu rincón dulce en Momomy</p>
                </div>
            </div>
            <a href="{{ base_url }}connect/logout" class="px-6 py-3 rounded-2xl bg-white border border-slate-200 text-slate-600 font-bold text-sm hover:bg-slate-50 transition-all text-center">
                Cerrar Sesión
            </a>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Orders Section -->
            <div class="lg:col-span-2 space-y-6">
                <div class="bg-white rounded-[2.5rem] p-8 border border-white/40 shadow-xl overflow-hidden">
                    <div class="flex items-center justify-between mb-8">
                        <h2 class="text-xl font-black text-slate-900">Mis Pedidos</h2>
                        <span class="px-3 py-1 bg-primary/10 text-primary text-[10px] font-black rounded-full">{{ orders|length }} TOTAL</span>
                    </div>

                    <div class="space-y-4">
                        {% for order in orders %}
                        <a href="{{ base_url }}connect/order/{{ order.tracking_token }}" class="flex items-center gap-4 p-4 rounded-3xl bg-slate-50 hover:bg-slate-100 transition-all group">
                            <div class="size-12 rounded-2xl bg-white flex items-center justify-center text-primary shadow-sm">
                                <span class="material-symbols-outlined">shopping_bag</span>
                            </div>
                            <div class="flex-grow min-w-0">
                                <p class="font-bold text-slate-900 truncate">Pedido #{{ "%06d"|format(order.id) }}</p>
                                <p class="text-xs text-slate-500">{{ order.created_at|date('d/m/Y') }}</p>
                            </div>
                            <div class="text-right">
                                <p class="font-black text-slate-900">${{ order.total_amount|number_format(2) }}</p>
                                <span class="text-[9px] font-black uppercase {% if order.status == 'completed' %}text-green-500{% else %}text-orange-500{% endif %}">
                                    {{ order.status|upper }}
                                </span>
                            </div>
                            <span class="material-symbols-outlined text-slate-300 group-hover:translate-x-1 transition-transform">chevron_right</span>
                        </a>
                        {% else %}
                        <div class="text-center py-12">
                            <p class="text-slate-400 font-medium">Aún no tienes pedidos.</p>
                        </div>
                        {% endfor %}
                    </div>
                </div>

                <div class="bg-white rounded-[2.5rem] p-8 border border-white/40 shadow-xl">
                    <h2 class="text-xl font-black text-slate-900 mb-8">Mis Cotizaciones</h2>
                    <div class="space-y-4">
                        {% for quote in quotations %}
                        <div class="p-4 rounded-3xl bg-slate-50 flex items-center justify-between">
                            <div class="flex items-center gap-4">
                                <div class="size-12 rounded-2xl bg-white flex items-center justify-center text-purple-500 shadow-sm">
                                    <span class="material-symbols-outlined">description</span>
                                </div>
                                <div>
                                    <p class="font-bold text-slate-900">{{ quote.subject }}</p>
                                    <p class="text-[10px] text-slate-500 uppercase font-black">{{ quote.status|upper }}</p>
                                </div>
                            </div>
                            {% if quote.status == 'sent' %}
                            <a href="{{ base_url }}admin/quotations/download/{{ quote.id }}" class="text-primary font-bold text-xs hover:underline">Ver PDF</a>
                            {% endif %}
                        </div>
                        {% else %}
                        <p class="text-center text-slate-400 py-4 font-medium">No hay cotizaciones registradas.</p>
                        {% endfor %}
                    </div>
                </div>
            </div>

            <!-- Sidebar Info -->
            <div class="space-y-6">
                <div class="bg-slate-900 text-white rounded-[2.5rem] p-8 shadow-2xl relative overflow-hidden">
                    <div class="absolute top-0 right-0 size-32 bg-primary/20 blur-3xl rounded-full -mr-16 -mt-16"></div>
                    <h3 class="text-lg font-black mb-4 flex items-center gap-2 relative z-10">
                        <span class="material-symbols-outlined text-primary">chat_bubble</span> Contacto Directo
                    </h3>
                    <p class="text-slate-400 text-xs font-medium mb-6 leading-relaxed relative z-10">¿Tienes un evento especial? Mándanos un mensaje y cotizamos tu idea.</p>
                    <a href="{{ base_url }}#contacto" class="block w-full py-4 bg-primary text-white rounded-2xl text-center font-black text-sm relative z-10 hover:scale-105 transition-transform">
                        NUEVO MENSAJE
                    </a>
                </div>

                <div class="bg-white rounded-[3rem] p-8 border border-white/40 shadow-xl text-center">
                    <div class="size-16 bg-green-500/10 text-green-500 rounded-2xl flex items-center justify-center mx-auto mb-4">
                        <span class="material-symbols-outlined text-3xl">verified</span>
                    </div>
                    <h4 class="font-bold text-slate-900 mb-2">Cliente VIP</h4>
                    <p class="text-xs text-slate-500 font-medium">Gracias por ser parte de la familia Momomy. ¡Pronto tendremos sorpresas para ti!</p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
