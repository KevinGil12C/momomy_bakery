{% extends "public/layout.twig" %}

{% block content %}
<section class="max-w-6xl mx-auto px-6 py-12 lg:py-20">
    <div class="text-center mb-16">
        <span class="inline-block px-4 py-2 rounded-full bg-primary/10 text-primary text-xs font-black mb-6 uppercase tracking-widest">Contáctanos</span>
        <h1 class="text-5xl md:text-7xl font-black mb-8 tracking-tighter text-slate-900 dark:text-white">
            Contacto y <span class="text-primary italic text-6xl md:text-8xl">Pedidos</span>
        </h1>
        <p class="text-slate-500 dark:text-slate-400 text-lg max-w-2xl mx-auto leading-relaxed font-medium">
            ¿Antojo de algo dulce? Estamos aquí para hacer tu día más mágico. Envíanos un mensaje o visítanos en nuestro acogedor rincón.
        </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 items-start">
        <div class="lg:col-span-7 bg-white dark:bg-slate-800 p-10 rounded-[3rem] shadow-2xl border border-primary/5">
            <h2 class="text-2xl font-black mb-10 flex items-center gap-3 dark:text-white italic">
                <span class="material-symbols-outlined text-primary text-3xl">edit_note</span> 
                Envíanos un Mensaje
            </h2>
            <form id="contact-form" class="space-y-8">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                    <div class="flex flex-col gap-3">
                        <label class="text-xs font-black uppercase tracking-widest text-slate-400 px-2 transition-colors">Tu Nombre</label>
                        <input name="name" class="w-full rounded-2xl border-none bg-background-light dark:bg-slate-700/50 focus:ring-4 focus:ring-primary/20 h-16 px-6 outline-none transition-all dark:text-white font-bold" placeholder="¿Cómo te llamas?" type="text" required/>
                    </div>
                    <div class="flex flex-col gap-3">
                        <label class="text-xs font-black uppercase tracking-widest text-slate-400 px-2">Correo Electrónico</label>
                        <input name="email" class="w-full rounded-2xl border-none bg-background-light dark:bg-slate-700/50 focus:ring-4 focus:ring-primary/20 h-16 px-6 outline-none transition-all dark:text-white font-bold" placeholder="tu@correo.com" type="email" required/>
                    </div>
                </div>
                <div class="flex flex-col gap-3">
                    <label class="text-xs font-black uppercase tracking-widest text-slate-400 px-2">¿En qué podemos ayudarte?</label>
                    <textarea name="message" class="w-full rounded-[2rem] border-none bg-background-light dark:bg-slate-700/50 focus:ring-4 focus:ring-primary/20 p-8 outline-none transition-all resize-none dark:text-white font-bold" placeholder="Cuéntanos tu antojo o duda... ✨" rows="6" required></textarea>
                </div>
                <button class="w-full md:w-auto px-12 py-5 bg-primary text-white rounded-3xl font-black text-lg hover:opacity-90 shadow-2xl shadow-primary/30 active:scale-95 transition-all flex items-center justify-center gap-3" type="submit">
                    ENVIAR FELICIDAD
                    <span class="material-symbols-outlined">send</span>
                </button>
            </form>
        </div>

        <div class="lg:col-span-5 flex flex-col gap-8">
            <!-- WhatsApp Card -->
            <a href="https://wa.me/{{ config.MAIL }}" target="_blank" class="group relative overflow-hidden bg-[#25D366] text-white p-10 rounded-[3rem] shadow-2xl transition-transform hover:-translate-y-2 active:scale-95 duration-500">
                <div class="absolute -top-10 -right-10 size-40 bg-white/10 rounded-full blur-3xl group-hover:scale-150 transition-transform duration-700"></div>
                <div class="relative z-10 flex items-center justify-between">
                    <div>
                        <h3 class="text-3xl font-black mb-2 italic tracking-tighter">WhatsApp</h3>
                        <p class="font-bold opacity-80 uppercase text-[10px] tracking-widest">Pedidos rápidos y dudas</p>
                    </div>
                    <span class="material-symbols-outlined text-6xl opacity-30 group-hover:rotate-12 transition-transform">forum</span>
                </div>
            </a>

            <!-- Info Card -->
            <div class="bg-white dark:bg-slate-800 p-10 rounded-[3rem] border border-primary/5 shadow-2xl space-y-8">
                <h3 class="text-2xl font-black flex items-center gap-3 dark:text-white italic">
                    <span class="material-symbols-outlined text-primary text-3xl">location_on</span> 
                    Visita Nuestra Pastelería
                </h3>
                <div class="w-full h-56 rounded-[2rem] overflow-hidden bg-slate-100 dark:bg-slate-700 shadow-inner group">
                    <img class="size-full object-cover grayscale group-hover:grayscale-0 transition-all duration-700 scale-110 group-hover:scale-100" src="https://images.unsplash.com/photo-1555507036-ab1f4038808a?auto=format&fit=crop&q=80&w=1000"/>
                </div>
                <div class="space-y-4">
                    <div class="flex items-start gap-4">
                        <span class="material-symbols-outlined text-primary mt-1">navigation</span>
                        <p class="text-slate-600 dark:text-slate-400 font-bold text-sm italic">{{ config.BUSINESS_ADDRESS }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}

{% block scripts %}
<script>
    document.getElementById('contact-form').addEventListener('submit', async (e) => {
        e.preventDefault();
        const formData = new FormData(e.target);
        const data = Object.fromEntries(formData.entries());

        try {
            const res = await fetch('{{ base_url }}api/contact', {
                method: 'POST',
                body: JSON.stringify(formData) // ApiController might expect form data or raw json
            });
            
            // Note: ApiController.receiveContact uses $_POST, so we should use FormData
            const response = await fetch('{{ base_url }}api/contact', {
                method: 'POST',
                body: formData
            });

            if (response.ok) {
                Swal.fire({
                    icon: 'success',
                    title: '¡Mensaje Enviado!',
                    text: 'Te contactaremos muy pronto. 🧁',
                    confirmButtonColor: '#f0427c'
                });
                e.target.reset();
            } else {
                throw new Error();
            }
        } catch (error) {
            Swal.fire('Error', 'No se pudo enviar el mensaje. Intenta más tarde.', 'error');
        }
    });
</script>
{% endblock %}
